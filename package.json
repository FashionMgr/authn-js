{
  "name": "@fashionmanger/authn-svc",
  "version": "0.1.0",
  "description": "Browser integration library for AuthN service.",
  "main": "./dist/authn.js",
  "module": "./dist/authn.module.js",
  "types": "./dist/index.d.ts",
  "dependencies": {},
  "devDependencies": {
    "gulp": "^4.0.2",
    "node-qunit-phantomjs": "^2.0.1",
    "rollup": "^0.49.3",
    "rollup-plugin-filesize": "^1.4.2",
    "rollup-plugin-uglify": "^2.0.1",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "release": "yarn problems && yarn test && yarn build && yarn publish",
    "problems": "yarn tsc --noEmit",
    "build": "yarn clean && yarn compile && rollup -c && cp lib/*.d.ts dist",
    "test": "gulp test",
    "compile": "yarn tsc",
    "clean": "rm -rf dist"
  }
}
